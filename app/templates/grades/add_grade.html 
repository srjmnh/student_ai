<!-- app/templates/grades/add_grade.html -->

{% extends 'base.html' %}

{% block content %}
<div class="container mt-4">
    <h2>Add Grade Entry</h2>
    <form method="POST" action="{{ url_for('grades.add_grade') }}">
        <div class="mb-3">
            <label for="subject" class="form-label">Subject Name *</label>
            <input type="text" class="form-control" id="subject" name="subject" required>
        </div>
        <div class="mb-3">
            <label for="grades" class="form-label">Grades (JSON Format)</label>
            <textarea class="form-control" id="grades" name="grades" rows="10" placeholder='{"student_id1": "A", "student_id2": "B+", "student_id3": "A-"}'></textarea>
            <div class="form-text">Enter grades as a JSON object with student IDs as keys and their grades as values.</div>
        </div>
        <button type="submit" class="btn btn-success">Add Grade Entry</button>
    </form>
</div>
{% endblock %}